# 住宅外観ジェネレーター

Nano Banana Pro（Gemini 3 Pro Image）で日本の注文住宅の外観を生成する、単一HTMLのツールです。
`index.html` を静的ホスティング（Vercel等）に置くだけで動作します。

## 一時公開でキーを埋め込む場合
`index.html` 内の `const EMBEDDED_KEY = "";` の "" にAPIキーを貼ってください。
※公開ソースから抜き取れるため、使い捨てキー＋予算/上限設定＋公開後削除を必ず行ってください。
